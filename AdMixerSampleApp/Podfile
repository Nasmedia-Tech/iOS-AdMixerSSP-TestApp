# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AdMixerSampleApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AdMixerSampleApp
  pod 'AdMixerMediation', '2.0.4'
  pod 'AdMixerMediationGAM', '1.0.1'
  pod 'AdMixerMediationAdFit', '1.0.2'
  pod 'AdMixerMediationMobWith', '1.0.1'
  pod 'AdMixerMediationPangle', '1.0.0'

  target 'AdMixerSampleAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AdMixerSampleAppUITests' do
    # Pods for testing
  end

end
